{
  "name": "@scrollpop/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-snippet": "node -e \"require('fs').copyFileSync('../../packages/snippet/dist/p.js', 'src/p.txt')\"",
    "dev": "pnpm copy-snippet && wrangler dev",
    "build": "pnpm copy-snippet && wrangler deploy --dry-run --outdir dist --env=\"\"",
    "deploy": "pnpm copy-snippet && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'ESLint runs via root workspace — no local eslint in worker'",
    "test": "vitest run"
  },
  "dependencies": {
    "@scrollpop/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.0.0"
  }
}
